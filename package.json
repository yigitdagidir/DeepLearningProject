{
  "name": "deeplearningproject-docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Deliverable rendering (report + slides -> PDF) for the multi-modal fashion classifier. Not part of the Python pipeline; used only to build the PDF deliverables.",
  "scripts": {
    "render:slides": "marp docs/PRESENTATION.md -o docs/PRESENTATION.pdf --allow-local-files",
    "render:report": "node scripts/render_report.mjs",
    "render:source": "node scripts/render_report.mjs docs/SOURCE_CODE.md",
    "render": "npm run render:slides && npm run render:report && npm run render:source"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^3.4.0",
    "katex": "^0.16.11",
    "markdown-it": "^14.1.0",
    "markdown-it-texmath": "^1.0.0",
    "puppeteer": "^23.6.0"
  }
}
